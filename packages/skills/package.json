{
  "name": "@ch4p/skills",
  "version": "0.1.6",
  "private": true,
  "description": "Skill discovery, YAML frontmatter, and OpenClaw-compatible registry",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ch4p/core": "workspace:*"
  }
}
