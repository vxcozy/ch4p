{
  "name": "@ch4p/tunnels",
  "version": "0.3.0",
  "private": true,
  "description": "Public URL tunnel providers — Cloudflare, Tailscale, ngrok",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ch4p/core": "workspace:*"
  }
}
