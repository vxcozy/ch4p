{
  "name": "@ch4p/engines",
  "version": "0.1.5",
  "private": true,
  "description": "Execution engines — native LLM, echo testing, and CLI subprocess wrappers",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ch4p/core": "workspace:*"
  }
}
