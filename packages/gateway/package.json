{
  "name": "@ch4p/gateway",
  "version": "0.1.0",
  "description": "HTTP server, session routing, WebSocket bridge, cron, and webhooks",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ch4p/canvas": "workspace:*",
    "@ch4p/core": "workspace:*",
    "ws": "^8.0.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1"
  }
}
