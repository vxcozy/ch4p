{
  "name": "ch4p",
  "version": "0.1.0",
  "description": "Personal AI assistant platform — OpenClaw's reach, Lemon's concurrency, ZeroClaw's discipline",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.29.3",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "vitest",
    "test:coverage": "vitest --coverage",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit",
    "bundle": "pnpm -r build && cd apps/cli && bun run build.ts",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "tsup": "^8.0.0"
  }
}
